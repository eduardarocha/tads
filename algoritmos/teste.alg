Algoritmo "atividade-08"
Var
g, r : vetor[1..20] de inteiro
i, id : inteiro

Inicio
para i de 1 ate 20 passo 1 faca
    escreva("g[", i, "] = ")
    leia(g[i])
fimpara

enquanto (0 <= id) faca
    escreva("ID: ")
    leia(id)
    para i de 1 ate 20 passo 1 faca
        escreva("r[", i, "] = ")
        leia(r[i])
    fimpara
fimenquanto
Fimalgoritmo